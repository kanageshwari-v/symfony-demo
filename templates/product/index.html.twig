{% extends 'base.html.twig' %}

{% block title %}employees{% endblock %}

{% block body %}
<div class="container mt-5">
   <h2>symfony  company employees</h2>
   <table class="table mt-3">
      <thead>
      <tr>
         <th scope="col">#</th>
         <th scope="col">Name</th>
         <th scope="col">salary</th>
         <th scope="col">Destation</th>
         <th scope="col">Delete</th>

      </tr>
      </thead>
      <tbody>
      {% if employees is defined %}
         {% for employees in employeescontroller %}
         <tr>
            <th scope="row">{{ employees.id }}</th>
            <td>{{ employee.name }}</td>
            <td>{{ employee.salary }}</td>
            <td>{{ employee.destation }}</td>
            <td><a href="{{ path('delete_employees',{id: employees.id}) }}" class="text-danger">Delete</a></td>
         </tr>
         {% endfor %}
      {% endif %}
      </tbody>
   </table>
   <a class="btn btn-primary" href="{{ path('create_employeescontroller')}}" role="button">Add a product</a>
</div>
{% endblock %}
